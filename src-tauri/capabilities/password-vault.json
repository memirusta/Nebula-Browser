{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "password-vault",
  "description": "Restricts encrypted password vault access to the trusted main shell",
  "webviews": ["main"],
  "permissions": ["allow-password-vault-commands"]
}
